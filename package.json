{
  "name": "population-atlas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "population-atlas — ES-module flag so 'node atlases/population/shared/test_*.js' can load the page modules under test. There is no npm dependency graph; the browser imports atlases/population/**/*.js directly."
}
